# Legacy PMT migration history

These migrations belong to the retired prototype Supabase database.

They are preserved unchanged for audit/history only. They are not a bootstrap chain: the first migration alters core tables that already existed in the prototype.

Do not apply these files to the new canonical PMT project. The executable migration chain for the new project lives in `supabase/migrations/`.