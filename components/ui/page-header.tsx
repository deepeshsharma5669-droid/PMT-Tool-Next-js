import Link from 'next/link'
export type BreadcrumbItem={label:string;href?:string}
export function Breadcrumbs({items}:{items:BreadcrumbItem[]}) { return <nav className="breadcrumbs" aria-label="Breadcrumb"><ol>{items.map((item,index)=><li key={`${item.label}-${index}`}>{index>0&&<span className="breadcrumbs__separator" aria-hidden="true">/</span>}{item.href?<Link href={item.href}>{item.label}</Link>:<span aria-current="page">{item.label}</span>}</li>)}</ol></nav> }
export function PageHeader({eyebrow,title,titleMeta,description,breadcrumbs,actions}:{eyebrow?:string;title:string;titleMeta?:React.ReactNode;description?:string;breadcrumbs?:BreadcrumbItem[];actions?:React.ReactNode}) { return <header className="page-header">{breadcrumbs&&<Breadcrumbs items={breadcrumbs}/>}<div className="page-header__row"><div>{eyebrow&&<p className="page-header__eyebrow">{eyebrow}</p>}<div className="page-header__title-line"><h1>{title}</h1>{titleMeta}</div>{description&&<p className="page-header__description">{description}</p>}</div>{actions&&<div className="page-header__actions">{actions}</div>}</div></header> }
