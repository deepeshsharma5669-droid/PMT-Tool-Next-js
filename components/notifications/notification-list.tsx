'use client'
import{useMemo,useState}from'react';import Link from'next/link';import type{Notification}from'@/lib/support/data';import{EmptyState}from'@/components/shared/states'
const present=(v:string)=>v.replace(/^CAMPAIGN_/,'PROJECT_').replace(/\bCampaigns?\b/gi,match=>match.toLowerCase().endsWith('s')?'Projects':'Project')
const label=(v:string)=>present(v).toLowerCase().replaceAll('_',' ').replace(/\b\w/g,x=>x.toUpperCase())
export function NotificationList({items}:{items:Notification[]}){const[scope,setScope]=useState('ALL'),[q,setQ]=useState('');const rows=useMemo(()=>items.filter(x=>(scope==='ALL'||scope==='UNREAD'&&!x.read||scope==='READ'&&x.read)&&(x.message+' '+x.type).toLowerCase().includes(q.toLowerCase())),[items,scope,q]);return <><div className="filter-bar"><label className="filter-search"><span>⌕</span><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search notifications"/></label><select value={scope} onChange={e=>setScope(e.target.value)}><option value="ALL">All notifications</option><option value="UNREAD">Unread</option><option value="READ">Read</option></select></div>{rows.length?<div className="notification-list">{rows.map(x=>{const target=x.action_code?.startsWith('/')?x.action_code:null,content=<><span className={`notification-mark ${x.read?'is-read':''}`}/><div><header><strong>{label(x.type)}</strong>{!x.read&&<b>Unread</b>}</header><p>{present(x.message)}</p><time>{new Intl.DateTimeFormat('en-IN',{dateStyle:'medium',timeStyle:'short'}).format(new Date(x.created_at))}</time></div><span>›</span></>;return target?<Link href={target} key={x.id}>{content}</Link>:<article key={x.id}>{content}</article>})}</div>:<EmptyState title="You're all caught up." description="No notifications match this view."/>}</>}
