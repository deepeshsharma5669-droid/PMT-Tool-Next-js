'use client'
import{useMemo,useState}from'react';import type{HierarchyActivity}from'@/lib/hierarchy/data';import{ActivityTimeline}from'@/components/hierarchy/workflow';import{EmptyState}from'@/components/shared/states'
export function ActivityExplorer({items}:{items:HierarchyActivity[]}){const[type,setType]=useState('ALL'),[q,setQ]=useState('');const rows=useMemo(()=>items.filter(x=>(type==='ALL'||x.entity_type===type)&&(x.action+' '+x.actorName).toLowerCase().includes(q.toLowerCase())),[items,type,q]);return <><div className="filter-bar"><label className="filter-search"><span>⌕</span><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search activity"/></label><select value={type} onChange={e=>setType(e.target.value)}><option value="ALL">All activity</option>{['CAMPAIGN','DELIVERABLE','STAGE','TASK','CLIENT'].map(x=><option key={x}>{x}</option>)}</select></div>{rows.length?<section className="hierarchy-primary activity-explorer"><ActivityTimeline items={rows}/></section>:<EmptyState title="No activity matches your filters."/>}</>}
