import type{ClientDecision}from'@/lib/hierarchy/data';import{StatusBadge}from'@/components/ui/badges';import{EmptyState}from'@/components/shared/states'
export function ClientDecisionHistory({history}:{history:ClientDecision[]}){if(!history.length)return <EmptyState title="No Client Decisions recorded yet."/>;return <div className="revision-history">{history.map((x,i)=><article key={x.id}><div className="revision-history__rail"><span>{x.client_revision}</span></div><div className="revision-history__content"><header><div><small>Revision {x.client_revision}</small><h3>{x.decision==='APPROVED'?'Client Approved':'Client Changes Requested'}</h3></div><StatusBadge status={x.decision}/></header><dl><div><dt>Date</dt><dd>{new Intl.DateTimeFormat('en-IN',{dateStyle:'medium'}).format(new Date(x.recorded_at))}</dd></div><div><dt>Channel</dt><dd>{x.channel??'Unavailable'}</dd></div><div><dt>Contact</dt><dd>{x.contact_person??'Unavailable'}</dd></div></dl>{x.feedback&&<blockquote>{x.feedback}</blockquote>}{x.notes&&<p>{x.notes}</p>}{i===0&&<b>Latest decision</b>}</div></article>)}</div>}
