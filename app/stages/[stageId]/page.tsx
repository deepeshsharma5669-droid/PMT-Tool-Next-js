import{notFound}from'next/navigation';import{requirePmtUser}from'@/lib/auth/session';import{AppShell}from'@/components/shell/app-shell';import{PageHeader}from'@/components/ui/page-header';import{StatusBadge}from'@/components/ui/badges';import{ActivityTimeline,StageProgress}from'@/components/hierarchy/workflow';import{PartialData,ProgressBar}from'@/components/shared/states';import{TaskWorkspace}from'@/components/tasks/task-workspace';import{ClientDecisionPanel}from'@/components/client-decision/client-decision-panel';import{ReworkSummary}from'@/components/client-decision/rework-summary';import{loadHierarchy,stageProgress}from'@/lib/hierarchy/data'
export default async function Page({params}:{params:Promise<{stageId:string}>}){const user=await requirePmtUser(),{stageId}=await params,data=await loadHierarchy(),stage=data.stages.find(x=>x.id===stageId);if(!stage)notFound();const d=data.deliverables.find(x=>x.id===stage.deliverable_id),c=data.campaigns.find(x=>x.id===d?.campaign_id),stages=data.stages.filter(x=>x.deliverable_id===stage.deliverable_id),tasks=data.tasks.filter(x=>x.stage_id===stage.id),decisions=data.decisions.filter(x=>x.deliverable_id===d?.id),nextStage=stages.find(x=>x.stage_order===stage.stage_order+1&&x.status==='ACTIVE'),activity=data.activity.filter(x=>x.entity_id===stage.id||tasks.some(t=>t.id===x.entity_id)),progress=stageProgress(stage.id,data);return <AppShell user={user}><div className="page-container"><PageHeader eyebrow={stage.dept} title={stage.name} description={`${d?.name??'Deliverable unavailable'} · ${c?.name??'Campaign unavailable'}`} breadcrumbs={[{label:'PMT',href:'/dashboard'},{label:'Campaigns',href:'/campaigns'},...(c?[{label:c.name,href:`/campaigns/${c.id}`}]:[]),...(d?[{label:d.name,href:`/deliverables/${d.id}`}]:[]),{label:stage.name}]} actions={<StatusBadge status={stage.rework_pending?'CLIENT_REWORK':stage.status}/>}/>{data.partial&&<PartialData/>}<div className="detail-overview"><div><span>Task progress</span><strong>{progress}%</strong><ProgressBar value={progress} label={false}/></div><div><span>Department</span><strong className="detail-text">{stage.dept}</strong><small>Owning team</small></div><div><span>Tasks</span><strong>{tasks.length}</strong><small>{tasks.filter(x=>x.status==='IN_REVIEW').length} awaiting review</small></div></div><section className="workflow-panel workflow-panel--compact"><StageProgress stages={stages} currentId={stage.id}/></section><ReworkSummary tasks={tasks} revision={d?.client_revision??0} active={stage.rework_pending}/><ClientDecisionPanel stage={stage} revision={d?.client_revision??0} user={user} history={decisions} nextStage={nextStage}/><TaskWorkspace currentRevision={d?.client_revision??0} reworkActive={stage.rework_pending} stageId={stage.id} dept={stage.dept} user={user} tasks={tasks} users={data.users}/><section className="hierarchy-aside stage-activity"><div className="section-heading"><h2>Activity & history</h2></div><ActivityTimeline items={activity}/></section></div></AppShell>}