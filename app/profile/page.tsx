import{requirePmtUser}from'@/lib/auth/session';import{AppShell}from'@/components/shell/app-shell';import{PageHeader}from'@/components/ui/page-header';import{RoleBadge,StatusBadge}from'@/components/ui/badges'
export default async function Page(){const user=await requirePmtUser();return <AppShell user={user}><div className="page-container"><PageHeader eyebrow="Account" title="Profile" description="Your PMT identity and workspace context." breadcrumbs={[{label:'PMT',href:'/dashboard'},{label:'Profile'}]}/><section className="profile-card profile-card--self"><div className="profile-avatar">{user.name.split(/\s+/).map(x=>x[0]).join('').slice(0,2)}</div><div><h2>{user.name}</h2><p>{user.email??'Email unavailable'}</p><div className="profile-badges"><RoleBadge role={user.role}/><StatusBadge status={user.status}/></div></div><dl><div><dt>Department</dt><dd>{user.dept??'All departments'}</dd></div><div><dt>Account status</dt><dd>{user.status.toLowerCase()}</dd></div><div><dt>Workspace</dt><dd>PMT Production</dd></div></dl></section></div></AppShell>}
