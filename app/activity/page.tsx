import{requirePmtUser}from'@/lib/auth/session';import{AppShell}from'@/components/shell/app-shell';import{PageHeader}from'@/components/ui/page-header';import{ActivityExplorer}from'@/components/activity/activity-explorer';import{PartialData}from'@/components/shared/states';import{loadActivity}from'@/lib/support/data';import{loadHierarchy}from'@/lib/hierarchy/data'
export default async function Page(){const user=await requirePmtUser(),[activity,hierarchy]=await Promise.all([loadActivity(),loadHierarchy()]);return <AppShell user={user}><div className="page-container"><PageHeader eyebrow={user.role==='ADMIN'?'Audit':'Workflow history'} title="Activity" description="Recorded PMT workflow events visible to your account." breadcrumbs={[{label:'PMT',href:'/dashboard'},{label:'Activity'}]}/>{(activity.error||hierarchy.partial)&&<PartialData/>}<ActivityExplorer items={activity.items} data={hierarchy}/></div></AppShell>}
